apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: truenas-ui
spec:
  secretName: truenas-ui-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
    - truenas.yiannis.io
