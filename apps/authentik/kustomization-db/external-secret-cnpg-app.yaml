apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: external-secret-authentik-cnpg-app
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: authentik-cnpg-app
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: kubernetes.io/basic-auth
      data:
        username: authentik
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: AUTHENTIK_POSTGRES_PASSWORD
