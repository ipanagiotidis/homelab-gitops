global:
  createDefaultServiceAccount: false

# Configure options applied to all pods
defaultPodOptions:
  automountServiceAccountToken: false

controllers:
  # Configure the main controller
  radarr:

    # Configure the pod-specific securityContext
    pod:
      securityContext:
        fsGroup: 1001
        fsGroupChangePolicy: "OnRootMismatch"
        seccompProfile:
          type: RuntimeDefault

    containers:
      # Configure the main application container
      radarr:
        image:
          # -- image repository
          repository: reg.yiannis.io/proxy-dockerhub/linuxserver/radarr
          # -- pinned to latest stable release tag: 6.3.0.10514-ls312
          digest: sha256:e35056574cdc695a9ee745aa1ecda9eab3842450bf4b7b8471b023790fa3861d
        # Configure the container-specific securityContext
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
            add:
              - SETGID
              - SETUID
        # -- Configure the resource requests and/or limits for the Pod
        resources:
          requests:
          #   # Hardware acceleration using an Intel iGPU w/ QuickSync and
          #   # using intel-gpu-plugin (https://github.com/intel/intel-device-plugins-for-kubernetes)
          #   gpu.intel.com/i915: 1
            cpu: 25m
            memory: 256Mi
          limits:
          #   # Hardware acceleration using an Intel iGPU w/ QuickSync and
          #   # using intel-gpu-plugin (https://github.com/intel/intel-device-plugins-for-kubernetes)
          #  gpu.intel.com/i915: 1
            memory: 1024Mi


service:
  # Configure a service for the main application
  controller: radarr
  radarr:
    type: ClusterIP
    ports:
      http:
        port: 7878

ingress:
  # Configure an Ingress for the main application
  radarr:
    enabled: false
    annotations:
      traefik.ingress.kubernetes.io/router.middlewares: traefik-authentik@kubernetescrd
      gethomepage.dev/enabled: "true"
      gethomepage.dev/description: Movie management
      gethomepage.dev/href: "https://rdr.yiannis.io"
      gethomepage.dev/group: Media
      gethomepage.dev/icon: radarr.png
      gethomepage.dev/name: Radarr
      gethomepage.dev/pod-selector: ""
      gethomepage.dev/widget.type: "radarr"
      gethomepage.dev/widget.url: "http://radarr.media-video.svc.cluster.local:7878"
      gethomepage.dev/widget.key: '{{`{{HOMEPAGE_VAR_RADARR}}`}}'
    ingressClassName: "traefik"
    hosts:
      - host: "rdr.yiannis.io"
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: radarr
              port: http

persistence:
  config:
    enabled: true
    type: persistentVolumeClaim
    storageClass: truenas-apps
    accessMode: ReadWriteOnce
    size: 2Gi
    globalMounts:
      - path: /config

  media:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: media-video
    globalMounts:
      - path: /data
