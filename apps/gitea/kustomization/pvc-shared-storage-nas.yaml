apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-shared-storage-nas
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-apps
