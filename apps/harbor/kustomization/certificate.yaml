apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-tls-certificate
  namespace: harbor
spec:
  secretName: harbor-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
  - reg.yiannis.io
