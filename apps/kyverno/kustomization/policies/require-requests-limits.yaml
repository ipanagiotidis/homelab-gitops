apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-requests-limits
  annotations:
    policies.kyverno.io/title: Require Limits and Requests
    policies.kyverno.io/category: Best Practices
    policies.kyverno.io/minversion: 1.6.0
    policies.kyverno.io/severity: medium
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      Application workloads should set CPU and memory requests, and at least memory limits,
      to improve scheduling predictability and cluster safety. This audit-only policy checks
      the first targeted namespaces. Init and ephemeral containers are intentionally excluded
      from the first audit pass to avoid low-value noise from chart-managed bootstrap helpers.
spec:
  admission: true
  validationFailureAction: Audit
  background: true
  emitWarning: false
  rules:
  - name: validate-resources
    match:
      any:
      - resources:
          kinds:
          - Pod
          namespaces:
          - authentik
          - gitea
          - homepage
          - monitoring
          - paperless-ngx
    skipBackgroundRequests: true
    validate:
      allowExistingViolations: true
      message: CPU and memory requests and memory limits are required for workload containers.
      pattern:
        spec:
          containers:
          - resources:
              requests:
                memory: '?*'
                cpu: '?*'
              limits:
                memory: '?*'
